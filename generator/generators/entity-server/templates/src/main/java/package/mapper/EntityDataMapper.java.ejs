package <%=packageName%>.mapper;

import com.datastax.oss.driver.api.core.CqlSession;
import com.datastax.oss.driver.api.mapper.MapperBuilder;
import com.datastax.oss.driver.api.mapper.annotations.DaoFactory;
import com.datastax.oss.driver.api.mapper.annotations.DaoKeyspace;
import com.datastax.oss.driver.api.mapper.annotations.Mapper;
import <%=packageName%>.dao.<%=asEntity(entityClass)%>Dao;

@Mapper
public interface <%=asEntity(entityClass)%>DataMapper {

    @DaoFactory
    <%=asEntity(entityClass)%>Dao <%=entityInstance%>Dao();

    @DaoFactory
    <%=asEntity(entityClass)%>Dao <%=entityInstance%>Dao(@DaoKeyspace String keyspace);

    static MapperBuilder<<%=asEntity(entityClass)%>DataMapper> builder(CqlSession session) {
        return new <%=asEntity(entityClass)%>DataMapperBuilder(session);
    }
}


